name: Function Tests (Docker)

on:
  workflow_call:
    inputs:
      function_name:
        description: "Имя функции для проверки (без расширения)"
        required: true
        type: string
    secrets:
      OPENROUTER_API_KEY:
        required: true

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      # Чекаутим репозиторий с функцией (репо А)
      - name: Checkout function repo
        uses: actions/checkout@v4

      # Чекаутим репозиторий с Dockerfile (репо B)
      - name: Checkout docker/tests repo
        uses: actions/checkout@v4
        with:
          repository: Smaipl/Tests-Function-Api-Calling-OpenAi
          path: tests-repo

      # Собираем Docker-образ из репо B
      - name: Build Docker image
        run: docker build -t function-tests tests-repo

      # Запускаем контейнер и монтируем код из репо А внутрь
      - name: Run tests in Docker
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          docker run --rm \
            -e OPENROUTER_API_KEY=$OPENROUTER_API_KEY \
            -v $GITHUB_WORKSPACE:/app \
            function-tests \
            --function "/app/${{ inputs.function_name }}.py" \
            --schema "/app/${{ inputs.function_name }}.json" \
            --tests "/app/${{ inputs.function_name }}.yaml"
